package entities.particle;

import spacial.Cardinal;
import flixel.FlxSprite;

class Dust extends FlxSprite {
	public function new(x:Float, y:Float, dir:Cardinal) {
		super(x, y);

		// loadGraphic(AssetPaths.)
	}
}